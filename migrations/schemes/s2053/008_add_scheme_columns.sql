-- Migration: 008_add_scheme_columns.sql
-- Status: OBSOLETE - scheme tables now created by ORM with scheme columns built-in
-- The scheme-specific models include scheme_code and sub_scheme_code columns directly.

SELECT 1;
